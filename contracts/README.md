# Contract Coverage

This directory stores machine-readable contract coverage metadata.

`adc.contract_coverage.v1.json` maps each public schema to the static,
generated, invariant, adversarial, and trace checks that keep it executable.
